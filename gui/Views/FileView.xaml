<UserControl x:Class="TreeChat.Views.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource CardBackground}" Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏：创建新文件和打开现有文件 -->
        <Border Grid.Row="0" Background="#F8FAFE" BorderBrush="#E2E6EA" BorderThickness="0,0,0,1" Padding="10,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="创建新文件" Command="{Binding CreateNewFile}" Margin="0,0,10,0" Padding="12,6"/>
                <Button Content="打开现有文件" Command="{Binding OpenExistingFile}" Padding="12,6"/>
            </StackPanel>
        </Border>

        <!-- 最近打开的文件 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="最近打开的文件" FontSize="14"
                      Foreground="#505E6C" Margin="0,0,0,8"/>
            <ListBox Grid.Row="1" ItemsSource="{Binding RecentFiles}"
                     Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="5,2" FontSize="13" Foreground="#1F2A3A"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
